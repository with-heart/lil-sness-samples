/** The `_base` value for `strudel.json`. */
export const BASE_URL = `https://raw.githubusercontent.com/with-heart/lil-sness-samples/main/samples/`

/** Prefix for all `strudel.json` keys. */
export const KEY_PREFIX = 'snes'
